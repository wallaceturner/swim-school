Build the swim-school Docker image and push to Docker Hub.

Run this:
1. `docker build -t wallaceturner/swimschool:latest /opt/github/wallaceturner/swim-school`
2. `docker push wallaceturner/swimschool:latest`
